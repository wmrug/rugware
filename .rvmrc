if [[ -n "$rvm_environments_path" && -s "$rvm_environments_path/ruby-1.9.2-p0@rugware" ]] ; then
  \. "$rvm_environments_path/ruby-1.9.2-p0@rugware"
else
  rvm --create use  "ruby-1.9.2-p0@rugware"
fi